# Phase 7E-4 / GraphRAG v2.1 — Redis keys & TTL policy

**Principles:** ephemeral cache only; no schema migrations required. Keys and TTLs should be safe to FLUSH for test DBs.

**Canonical keys observed/inferred:**
- `REDIS_URL` (TTL: 10m–24h depending on workload)
- `rag:v1` (TTL: 10m–24h depending on workload)
- `redis-cli` (TTL: 10m–24h depending on workload)
- `redis-url` (TTL: 10m–24h depending on workload)
- `redis_client` (TTL: 10m–24h depending on workload)
- `redis_epoch_bump` (TTL: 10m–24h depending on workload)
- `redis_invalidate` (TTL: 10m–24h depending on workload)
- `redis_invalidation` (TTL: 10m–24h depending on workload)
- `redis_url` (TTL: 10m–24h depending on workload)

**Test-only flush:** If `REDIS_DB=1` is used for tests, it's safe to run `FLUSHDB ASYNC` on that DB. Avoid flushing production DBs.
